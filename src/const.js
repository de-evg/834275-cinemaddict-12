const DescCount = {
  MIN: 1,
  MAX: 5
};

const CommentCount = {
  MIN: 0,
  MAX: 5
};

const Year = {
  MIN: 1900,
  MAX: 2020
};

const GenresCount = {
  MIN: 1,
  MAX: 3
};

const SortType = {
  DEFAULT: `default`,
  RELEASE: `release`,
  RATING: `rating`
};

export {DescCount, CommentCount, Year, GenresCount, SortType};
